---
phase: 03-auth-sharing
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/hooks/useAuth.tsx
  - src/components/auth/PasscodeModal.tsx
  - src/components/auth/EditToggle.tsx
  - src/app/schedule/[slug]/ScheduleClient.tsx
  - src/app/layout.tsx
  - src/components/task-list/TaskTable.tsx
  - src/components/task-list/TaskRow.tsx
  - src/components/owners/OwnerManager.tsx
  - src/components/gantt/GanttView.tsx
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03]

must_haves:
  truths:
    - "Visiting the schedule URL shows the full read-only view without any login prompt"
    - "Edit controls (Add Task, owner manager, inline fields, drag handles) are hidden in read-only mode"
    - "A visible lock/unlock button is always present in the header"
    - "Clicking unlock opens a passcode modal; correct passcode reveals edit controls"
    - "After unlocking, a lock button lets the user switch back to read-only mode"
    - "After entering the correct passcode once, the user stays authenticated across browser sessions"
    - "Mutations use the editor Supabase client (JWT); read-only uses anon client"
  artifacts:
    - path: "src/hooks/useAuth.tsx"
      provides: "AuthProvider context with isEditor, isLoading, unlock, lock"
      exports: ["AuthProvider", "useAuth"]
    - path: "src/components/auth/PasscodeModal.tsx"
      provides: "Modal dialog for passcode entry"
      exports: ["PasscodeModal"]
    - path: "src/components/auth/EditToggle.tsx"
      provides: "Lock/unlock button component"
      exports: ["EditToggle"]
  key_links:
    - from: "src/hooks/useAuth.tsx"
      to: "/api/auth/verify"
      via: "fetch POST on unlock"
      pattern: "fetch.*api/auth/verify"
    - from: "src/hooks/useAuth.tsx"
      to: "/api/auth/status"
      via: "fetch GET on mount"
      pattern: "fetch.*api/auth/status"
    - from: "src/hooks/useAuth.tsx"
      to: "/api/auth/lock"
      via: "fetch POST on lock"
      pattern: "fetch.*api/auth/lock"
    - from: "src/app/schedule/[slug]/ScheduleClient.tsx"
      to: "src/hooks/useAuth.tsx"
      via: "useAuth hook for isEditor check"
      pattern: "useAuth"
    - from: "src/app/schedule/[slug]/ScheduleClient.tsx"
      to: "src/lib/supabase/editor-client.ts"
      via: "createEditorClient for mutations when isEditor"
      pattern: "createEditorClient"
    - from: "src/app/layout.tsx"
      to: "src/hooks/useAuth.tsx"
      via: "AuthProvider wrapping children"
      pattern: "AuthProvider"
---

<objective>
Wire auth state into the UI: add AuthProvider context, passcode modal, edit toggle button, and conditionally show/hide all edit controls based on isEditor state. Mutations switch to editor Supabase client when authenticated.

Purpose: Complete the user-facing auth experience — contractors see a clean read-only view, homeowners unlock editing with a passcode that persists across sessions.

Output: Working passcode-gated editing with read-only default, visible lock/unlock toggle, and session persistence via localStorage + httpOnly cookie.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-auth-sharing/03-CONTEXT.md
@.planning/phases/03-auth-sharing/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs — executor uses these contracts directly -->

From src/lib/auth/jwt.ts (created in Plan 01):
```typescript
export function signEditorToken(): Promise<string>;
export function verifyEditorToken(token: string): Promise<boolean>;
```

From src/lib/supabase/editor-client.ts (created in Plan 01):
```typescript
export function createEditorClient(editorToken: string): SupabaseClient;
```

From src/app/api/auth/* (created in Plan 01):
- POST /api/auth/verify — body: { passcode: string } — returns 200 { success: true } + sets cookie, or 401 { error: string }
- GET /api/auth/status — returns { authenticated: boolean, token?: string }
- POST /api/auth/lock — clears cookie — returns { success: true }

From src/hooks/useAuth.tsx (to be created in this plan):
```typescript
interface AuthState {
  isEditor: boolean;
  isLoading: boolean;
  editorToken: string | null;
  unlock: (passcode: string) => Promise<{ success: boolean; error?: string }>;
  lock: () => void;
}
export function AuthProvider({ children }: { children: React.ReactNode }): JSX.Element;
export function useAuth(): AuthState;
```

From src/app/schedule/[slug]/ScheduleClient.tsx (created in Plan 01):
```typescript
// Client component with projectId prop — currently uses createClient() (anon) for ALL operations
// This plan will update it to use createEditorClient when isEditor === true
export default function ScheduleClient({ projectId }: { projectId: string }): JSX.Element;
```

Existing components that need isEditor gating:
- src/components/task-list/TaskTable.tsx — has add/delete buttons, inline editing
- src/components/task-list/TaskRow.tsx — has inline edit fields, delete button, dependency input
- src/components/owners/OwnerManager.tsx — has create/edit/delete UI
- src/components/gantt/GanttView.tsx — has drag-and-drop link creation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AuthProvider, PasscodeModal, and EditToggle components</name>
  <files>
    src/hooks/useAuth.tsx
    src/components/auth/PasscodeModal.tsx
    src/components/auth/EditToggle.tsx
    src/app/layout.tsx
  </files>
  <action>
    **Create `src/hooks/useAuth.tsx`:**
    - 'use client' directive
    - Create AuthContext with createContext
    - AuthProvider component that manages state:
      - `isEditor` (boolean, default false)
      - `isLoading` (boolean, default true — prevents flash of wrong state)
      - `editorToken` (string | null — the JWT for creating editor Supabase client)
    - On mount (useEffect), check localStorage for `schedule-editor === 'true'`:
      - If true, call `GET /api/auth/status` to validate cookie
      - The status endpoint should return `{ authenticated: boolean, token?: string }` — update: modify the status route to also return the token from the cookie so the client can use it for the editor Supabase client
      - If valid, set isEditor=true and store the token. If invalid, clear localStorage, set isEditor=false
      - If localStorage is not set, set isLoading=false immediately
    - `unlock(passcode: string)`: POST to /api/auth/verify, if 200: set localStorage `schedule-editor=true`, call status to get token, set isEditor=true. Return { success: true }. If 401: return { success: false, error: 'Invalid passcode' }
    - `lock()`: clear localStorage, set isEditor=false, editorToken=null, POST to /api/auth/lock to clear cookie
    - Export `useAuth` hook via useContext

    **IMPORTANT for editorToken flow:** After successful verify, immediately call GET /api/auth/status to retrieve the token value. The status endpoint reads the httpOnly cookie and returns it as `token` in the response. This way the client never sees the raw cookie but gets the JWT for creating the editor Supabase client. Update the status API route from Plan 01 to return `{ authenticated: boolean, token?: string }` — read the cookie value and include it when authenticated.

    **Create `src/components/auth/PasscodeModal.tsx`:**
    - Modal/dialog for passcode entry using shadcn Dialog component
    - Controlled by `open` prop and `onOpenChange` callback
    - Single password input field + Submit button
    - On submit: calls `unlock(passcode)` from useAuth context
    - Shows error toast via sonner on invalid passcode
    - Closes automatically on successful unlock
    - Shows loading state on submit button while verifying

    **Create `src/components/auth/EditToggle.tsx`:**
    - Uses `useAuth()` to get isEditor state
    - Renders a button with Lock icon (lucide-react) when in read-only mode, Unlock icon when in edit mode
    - Per user decision: "Visible unlock button (lock/edit icon) always present so authenticated users know editing is available"
    - When read-only: clicking opens the PasscodeModal (manages its own open state)
    - When editor: clicking calls `lock()` to switch back to read-only
    - Tooltip or title text: "Unlock editing" / "Lock editing"
    - Per user decision: "After unlocking, a lock button allows voluntarily switching back to read-only mode — useful for previewing the contractor view"

    **Update `src/app/layout.tsx`:**
    - Import AuthProvider from src/hooks/useAuth.tsx
    - Wrap {children} with AuthProvider inside the body tag (above or around existing content)
    - Keep Toaster as-is
  </action>
  <verify>
    <automated>cd /Users/lgillis/Downloads/Claude\ Code\ Projects/GANNT && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - AuthProvider manages isEditor, isLoading, editorToken state
    - On mount validates existing session via /api/auth/status
    - unlock() calls /api/auth/verify, stores session, retrieves token
    - lock() clears session and cookie
    - PasscodeModal provides passcode entry with error handling
    - EditToggle shows lock/unlock icon and manages modal open state
    - AuthProvider wraps app in layout.tsx
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire auth state into ScheduleClient and existing components</name>
  <files>
    src/app/schedule/[slug]/ScheduleClient.tsx
    src/app/api/auth/status/route.ts
    src/components/task-list/TaskTable.tsx
    src/components/task-list/TaskRow.tsx
    src/components/owners/OwnerManager.tsx
    src/components/gantt/GanttView.tsx
  </files>
  <action>
    **Update `src/app/api/auth/status/route.ts`:**
    - When authenticated, also return the token value from the cookie: `{ authenticated: true, token: cookieValue }`
    - This allows the client to use the token for createEditorClient without directly accessing httpOnly cookies

    **Update `src/app/schedule/[slug]/ScheduleClient.tsx`:**
    - Import `useAuth` from `src/hooks/useAuth.tsx`
    - Import `createEditorClient` from `src/lib/supabase/editor-client.ts`
    - Import `EditToggle` from `src/components/auth/EditToggle.tsx`
    - Get `{ isEditor, isLoading: authLoading, editorToken }` from useAuth()
    - Create the Supabase client conditionally:
      - When `isEditor && editorToken`: use `createEditorClient(editorToken)` for mutation handlers
      - When not editor: use `createClient()` (anon) — sufficient for reads
    - Update: use `useMemo` to memoize the client based on `isEditor` and `editorToken` to avoid recreating on every render
    - Add `EditToggle` to the header area (next to the weekend toggle / Add Task buttons)
    - Conditionally render edit controls based on `isEditor`:
      - `isEditor === false`: Hide "Add Task" button, hide weekend toggle, hide OwnerManager. Show only the project title, EditToggle, and read-only schedule view.
      - `isEditor === true`: Show everything (current behavior)
    - Pass `isEditor` prop down to TaskTable, GanttView, and OwnerManager

    **Update `src/components/task-list/TaskTable.tsx`:**
    - Accept `isEditor?: boolean` prop (default false for safety)
    - When `isEditor === false`:
      - Hide "Add Task" button (if present in table)
      - Hide delete buttons on rows
      - Pass `isEditor` to TaskRow

    **Update `src/components/task-list/TaskRow.tsx`:**
    - Accept `isEditor?: boolean` prop
    - When `isEditor === false`:
      - Render all fields as static text (not click-to-edit inputs)
      - Hide delete button, hide dependency edit input
      - Hide "Add Subtask" button
      - Keep the visual formatting (tier depth, colors) for read-only display

    **Update `src/components/owners/OwnerManager.tsx`:**
    - Accept `isEditor?: boolean` prop
    - When `isEditor === false`: hide the entire owner manager button (or show read-only owner list if desired — but per user decision, edit controls should be "completely hidden", so hide the button entirely)

    **Update `src/components/gantt/GanttView.tsx`:**
    - Accept `isEditor?: boolean` prop
    - When `isEditor === false`:
      - Do NOT wire onAddLink/onDeleteLink handlers (pass undefined or no-op)
      - CSS: hide link drag circles (`.wx-link-point { display: none; }`)
      - Gantt remains interactive for scrolling, hover, expand/collapse (per user decision: "contractors can hover for tooltips, expand/collapse task groups, scroll/zoom the timeline")
    - When `isEditor === true`: wire handlers as current behavior

    **Per user decisions being implemented:**
    - "Interactive read-only mode: contractors can hover for tooltips, expand/collapse task groups, scroll/zoom the timeline — just can't edit"
    - "Edit controls (input fields, drag handles, delete buttons) are completely hidden in read-only mode — clean, simplified view"
    - "Visible unlock button (lock/edit icon) always present so authenticated users know editing is available"
    - "After unlocking, a lock button allows voluntarily switching back to read-only mode"
  </action>
  <verify>
    <automated>cd /Users/lgillis/Downloads/Claude\ Code\ Projects/GANNT && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - ScheduleClient uses editor Supabase client for mutations when authenticated
    - EditToggle visible in header at all times
    - Read-only mode hides: Add Task, OwnerManager, inline edit fields, delete buttons, drag handles, link circles
    - Read-only mode preserves: scrolling, hover, expand/collapse, visual formatting
    - Edit mode reveals all controls and uses editor JWT for Supabase mutations
    - Switching between modes works without page reload
    - TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify auth flow end-to-end</name>
  <files>n/a</files>
  <action>
Human verification checkpoint. All code is already built in Tasks 1-2.

Complete passcode-gated editing with read-only default view, lock/unlock toggle, session persistence, and Supabase RLS enforcement.

**Verify the following:**

1. **Read-only default:** Visit /schedule/kitchen-reno — should see full schedule without login prompt. Verify:
   - No "Add Task" button visible
   - No inline edit fields (all text is static)
   - No delete buttons on tasks
   - No owner manager button
   - Gantt chart scrolls and task groups expand/collapse
   - Lock/unlock icon button IS visible in header

2. **Passcode unlock:** Click the unlock icon → enter passcode → submit. Verify:
   - Correct passcode: edit controls appear (Add Task, inline fields, delete buttons, owner manager)
   - Incorrect passcode: error toast appears, stays in read-only mode

3. **Edit mode works:** While unlocked, try:
   - Edit a task title inline
   - Create a new task
   - The operations should succeed (editor JWT used for mutations)

4. **Lock button:** Click the lock icon → verify edit controls disappear, returns to clean read-only view

5. **Session persistence:** Refresh the page → should still be in edit mode (no re-enter passcode). Close browser, reopen → still in edit mode.

6. **RLS enforcement (optional advanced test):** Open browser console, create a new anon Supabase client, try an INSERT → should get permission error.

Type "approved" or describe issues to fix.
  </action>
  <verify>Human visual and functional verification of auth flow</verify>
  <done>
    - Read-only default shows clean view without edit controls
    - Passcode unlock reveals all edit controls
    - Lock button returns to read-only view
    - Session persists across browser refresh
    - Mutations work in edit mode, blocked in read-only
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero errors
2. AuthProvider wraps app in layout.tsx
3. useAuth provides isEditor, unlock, lock functions
4. PasscodeModal and EditToggle render correctly
5. All edit controls hidden when isEditor === false
6. All edit controls visible when isEditor === true
7. Mutations use editor Supabase client (JWT) when authenticated
8. Session persists across browser refreshes (localStorage + cookie)
</verification>

<success_criteria>
- Visiting /schedule/kitchen-reno shows full read-only view without login prompt (AUTH-02)
- Correct passcode unlocks editing; incorrect passcode shows error (AUTH-01)
- Auth persists across browser sessions without re-entering passcode (AUTH-03)
- Lock button switches back to read-only mode (user decision)
- Unlock button always visible (user decision)
- Edit controls completely hidden in read-only mode (user decision)
- Interactive features (scroll, hover, expand/collapse) work in read-only mode (user decision)
- No regressions in task/owner/dependency CRUD when in edit mode
</success_criteria>

<output>
After completion, create `.planning/phases/03-auth-sharing/03-02-SUMMARY.md`
</output>
